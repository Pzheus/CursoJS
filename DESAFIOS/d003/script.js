function calculo(){
    var num = prompt("Digite um número inteiro")
    var ant = Number(num) - 1 
    var suc = Number(num) + 1

    alert(`Antes de ${num}, temos o número ${ant}. Depois de ${num} temos o número ${suc}`)
}